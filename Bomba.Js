
    var i = 30;
    var exploçao = 0;

    while(i > exploçao)
    {
        var texto = ("vai explodir em... "  + i + "<BR>");
        console.log (texto);
        document.write (texto);
        i--

    }
    var text = ("explodiu");

    console.log  (text);
    document.write (text);


